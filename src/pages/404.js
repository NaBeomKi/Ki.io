import React from "react";
import { App, NotFound } from "../components";

// markup
const NotFoundPage = () => {
  return (
    <App title="Not Found">
      <NotFound />
    </App>
  );
};

export default NotFoundPage;
